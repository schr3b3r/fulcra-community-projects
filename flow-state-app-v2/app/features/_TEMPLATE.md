# Feature: <name>

## Status
not_started | in_progress | done

## Description
What this feature does and why it matters, in plain language.

## Acceptance Criteria
- [ ] Concrete, checkable condition that defines "done".
- [ ] Another one.
- [ ] Has automated tests (pytest) covering the above criteria, and the
      FULL test suite passes (not just this feature's own tests) — see
      `app/ENGINEERING_STANDARDS.md`. This line should be present on every
      feature; do not remove it.

## Dependencies
Other features (by filename, e.g. `backend_skeleton.md`) this one depends
on, or "none".

## Notes
Anything else worth recording — open questions, known tradeoffs, things
deliberately deferred.
